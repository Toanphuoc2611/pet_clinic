import 'package:flutter/widgets.dart';

class FormInformationUser extends StatelessWidget {
  const FormInformationUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
